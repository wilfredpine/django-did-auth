[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "django-did-auth"
version = "0.1.2"
description = "Secure, pluggable authentication framework for Django"
readme = "README.md"
requires-python = ">=3.10"

license = { text = "MIT" }

authors = [
    { name = "Wilfred Pine", email = "wilfredpine9@gmail.com" }
]

keywords = ["django", "authentication", "security", "auth"]

classifiers = [
    "Framework :: Django",
    "Framework :: Django :: 4.2",
    "Framework :: Django :: 5.0",
    "Framework :: Django :: 5.1",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
]

dependencies = [
    "Django>=4.2",
    "django-ratelimit>=4.1.0",
    "django-redis>=5.0.0",      
    "Pillow>=10.0.0",           
    "django-axes>=8.0",         
    "argon2-cffi>=21.3.0",
]

[project.urls]
Homepage = "https://github.com/wilfredpine/django-did-auth"
Repository = "https://github.com/wilfredpine/django-did-auth"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["django_did_auth*"]